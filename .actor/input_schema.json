{
    "title": "Avvo Lawyers Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Direct Avvo lawyer directory URL to scrape from (e.g., https://www.avvo.com/bankruptcy-debt-lawyer/al.html).",
            "editor": "textfield",
            "prefill": "https://www.avvo.com/bankruptcy-debt-lawyer/al.html"
        },
        "maxLawyers": {
            "title": "Maximum Lawyers",
            "type": "integer",
            "description": "Maximum number of lawyer profiles to scrape (0 = unlimited). Default is 50 for optimal performance.",
            "default": 50,
            "minimum": 0,
            "maximum": 10000
        },
        "includeContactInfo": {
            "title": "Include Contact Information",
            "type": "boolean",
            "description": "Extract email addresses, phone numbers, and office addresses. Recommended for lead generation.",
            "default": true
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings for bypassing anti-bot protection. Highly recommended for reliable scraping.",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true
            }
        }
    },
    "required": [
        "startUrl"
    ]
}